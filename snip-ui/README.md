# snip-ui

`snip-ui` contains rendering backends that turn view models into terminal output.

## Responsibilities

- Renderer interfaces and backend contracts
- ANSI rendering implementation
- Presentation-layer output composition