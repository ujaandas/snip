# snip-file

`snip-file` provides stateless filesystem helpers for loading and saving editor buffers.

## Responsibilities

- Read full files and line ranges
- Write buffer contents to disk
- Surface I/O failures via exceptions for caller-level mapping
