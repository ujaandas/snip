# snip-term

`snip-term` manages terminal session lifecycle and terminal capability helpers.

## Responsibilities

- Enter/exit terminal session modes
- Query viewport size safely
- Write terminal control/output bytes
